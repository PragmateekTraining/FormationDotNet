namespace ValueTypesSamples
{
    class Program
    {
        static void Main(string[] args)
        {
            new AllocationSemanticsSample().Run();
            // new BasicSample().Run();
            // new BoxingPerformanceImpactSample().Run();
            // new BulkAllocationSample().Run();
            // new DefaultComparisonSample().Run();
            // new DictionaryKeySample().Run();
            // new EqualsOverloadSample(10000000).Run();
            // new InterfaceImplementationSample().Run();
            // new MutableValueSample().Run();
            // new ReadOnlyValueSample().Run();
        }
    }
}
