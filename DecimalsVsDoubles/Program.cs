namespace DecimalsSamples
{
    class Program
    {
        static void Main(string[] args)
        {
            // new PerformanceSample().Run();
            // new PrecisionSample(int.Parse(args[0])).Run();
            new RangeSample().Run();
        }
    }
}
